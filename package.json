{
  "name": "authelia-auth-proxy",
  "version": "1.0.0",
  "description": "Authentication proxy changing Authorization headers into Proxy-Authorization headers",
  "main": "index.js",
  "scripts": {
    "build": "rimraf ./dist && tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Sander Cox",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.17.13",
    "rimraf": "^3.0.2",
    "tslint": "^6.1.3",
    "typescript": "^4.3.5"
  },
  "dependencies": {
    "axios": "^0.21.1",
    "express": "^4.17.1"
  }
}
